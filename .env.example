# Codex Environment Configuration
# Copy this file to .env and fill in your values
# 使用命令: Copy-Item .env.example .env (PowerShell) 或 cp .env.example .env (Linux/Mac)

# ============================================
# 必需配置
# ============================================

# NovelAI API Token (必填)
CODEX_NAI_TOKEN=your_nai_token_here

# ============================================
# 可选配置 (用于自定义开发环境)
# ============================================
# 注意: start-dev.ps1 和 start-dev.py 脚本会自动配置以下变量
#       通常情况下不需要手动修改这些配置

# 服务监听地址 (默认: 127.0.0.1:8080)
# CODEX_ADDR=127.0.0.1:8080

# 数据库文件路径 (默认: .dev/codex.redb)
# CODEX_DB_PATH=.dev/codex.redb

# 预览图片目录 (默认: .dev/previews)
# CODEX_PREVIEW_DIR=.dev/previews

# 画廊目录 (默认: .dev/gallery)
# CODEX_GALLERY_DIR=.dev/gallery

# 静态文件目录 (仅在release模式下使用，默认: .dev/static)
# CODEX_STATIC_DIR=.dev/static

# 日志级别 (可选: trace, debug, info, warn, error)
# RUST_LOG=info

